commit 8ac827a4818a0ef2359ca9c6ca081e42273634a8
Author: = <=>
Date:   Mon Jan 5 01:46:03 2026 +0530

    refactor: Simplify API response structure in getProjects, getSortedArticles, getProjectBySlug, and getArticleData functions by explicitly constructing response objects without the createdAt field, ensuring consistent data representation.

diff --git a/Portfolio/src/lib/api.ts b/Portfolio/src/lib/api.ts
index 62f153e..5af6fb3 100644
--- a/Portfolio/src/lib/api.ts
+++ b/Portfolio/src/lib/api.ts
@@ -7,11 +7,18 @@ export async function getProjects() {
     await dbConnect();
     const projects = await Project.find({}).sort({ title : -1 }).lean();
     return projects.map((p: any) => {
-      const { createdAt, ...project } = p; // Remove createdAt
-      return {
-        ...project, 
-        _id: p._id.toString()
+      // Create new object without createdAt to ensure it's completely removed
+      const result: any = {
+        _id: p._id?.toString() || p._id,
+        slug: p.slug,
+        title: p.title,
+        description: p.description,
+        tags: p.tags,
       };
+      if (p.link) result.link = p.link;
+      if (p.github) result.github = p.github;
+      if (p.image) result.image = p.image;
+      return result;
     });
   } catch (error) {
     console.error('Error fetching projects:', error);
@@ -24,11 +31,18 @@ export async function getSortedArticles() {
     await dbConnect();
     const articles = await Article.find({}).sort({ slug: -1 }).lean();
     return articles.map((a: any) => {
-      const { createdAt, ...article } = a; // Remove createdAt
-      return {
-        ...article, 
-        _id: a._id.toString()
+      // Create new object without createdAt to ensure it's completely removed
+      const result: any = {
+        _id: a._id?.toString() || a._id,
+        slug: a.slug,
+        title: a.title,
+        date: a.date,
+        description: a.description,
+        tags: a.tags,
+        content: a.content,
       };
+      if (a.coverImage) result.coverImage = a.coverImage;
+      return result;
     });
   } catch (error) {
     console.error('Error fetching articles:', error);
@@ -41,11 +55,19 @@ export async function getProjectBySlug(slug: string) {
     await dbConnect();
     const project = await Project.findOne({ slug }).lean();
     if (!project) return null;
-    const { createdAt, ...projectData } = project as any; // Remove createdAt
-    return {
-        ...projectData, 
-        _id: (project as any)._id.toString()
+    const p = project as any;
+    // Create new object without createdAt to ensure it's completely removed
+    const result: any = {
+      _id: p._id?.toString() || p._id,
+      slug: p.slug,
+      title: p.title,
+      description: p.description,
+      tags: p.tags,
     };
+    if (p.link) result.link = p.link;
+    if (p.github) result.github = p.github;
+    if (p.image) result.image = p.image;
+    return result;
   } catch (error) {
     console.error('Error fetching project:', error);
     return null; // Return null if DB connection fails
@@ -57,11 +79,19 @@ export async function getArticleData(slug: string) {
     await dbConnect();
     const article = await Article.findOne({ slug }).lean();
     if (!article) return null;
-    const { createdAt, ...articleData } = article as any; // Remove createdAt
-    return {
-        ...articleData, 
-        _id: (article as any)._id.toString()
+    const a = article as any;
+    // Create new object without createdAt to ensure it's completely removed
+    const result: any = {
+      _id: a._id?.toString() || a._id,
+      slug: a.slug,
+      title: a.title,
+      date: a.date,
+      description: a.description,
+      tags: a.tags,
+      content: a.content,
     };
+    if (a.coverImage) result.coverImage = a.coverImage;
+    return result;
   } catch (error) {
     console.error('Error fetching article:', error);
     return null; // Return null if DB connection fails
